% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dist.structure-package.R
\docType{package}
\name{dist.structure-package}
\alias{dist.structure}
\alias{dist.structure-package}
\title{dist.structure: Structured Random Variables for Reliability System
Distributions}
\description{
Extends the algebraic.dist distribution algebra to random variables with
internal structure: coherent reliability systems decomposed into components
arranged by a structure function (series, parallel, k-out-of-n, bridge,
arbitrary topologies). Every \code{dist_structure} object is also a \code{dist}, so
the full distribution algebra works automatically via default methods that
compose component-level distributions through the topology.
}
\section{Two algebras, one hierarchy}{


\code{algebraic.dist} provides the base algebra of random variables:
\code{density}, \code{surv}, \code{hazard}, \code{sampler}, \code{mean}, \code{vcov}, \code{expectation},
and arithmetic on RVs (transformations).

\code{dist.structure} extends it with structure-preserving operations for
reliability systems: component-wise decomposition, structure function
evaluation, path and cut sets, system signature, critical states, dual
structures, and Birnbaum importance measures. Because every
\code{dist_structure} is a \code{dist}, the base algebra inherits automatically.
}

\section{Primitives implementors provide}{

\itemize{
\item \code{\link[=ncomponents]{ncomponents()}}: number of components \code{m}
\item \code{\link[=component]{component()}}: return the j-th component as a \code{dist}
\item One of \code{\link[=phi]{phi()}} or \code{\link[=min_paths]{min_paths()}}: the other defaults automatically
}
}

\section{Topology queries (default methods on \code{dist_structure})}{


\code{\link[=phi]{phi()}}, \code{\link[=min_paths]{min_paths()}}, \code{\link[=min_cuts]{min_cuts()}}, \code{\link[=critical_states]{critical_states()}},
\code{\link[=system_signature]{system_signature()}}, \code{\link[=system_lifetime]{system_lifetime()}}, \code{\link[=system_censoring]{system_censoring()}},
\code{\link[=dual]{dual()}}, \code{\link[=is_coherent]{is_coherent()}}, \code{\link[=structural_importance]{structural_importance()}}, \code{\link[=reliability]{reliability()}}
}

\section{Distribution queries (default methods via component composition)}{


\code{\link[algebraic.dist:surv]{algebraic.dist::surv()}}, \code{\link[algebraic.dist:cdf]{algebraic.dist::cdf()}}, \code{\link[algebraic.dist:sampler]{algebraic.dist::sampler()}}. Further base dist methods (\code{mean},
\code{vcov}, \code{expectation}) inherit from \code{univariate_dist} via numerical
integration of \code{surv}. Implementors override any default for speed.
}

\section{Reference implementations}{

\itemize{
\item \code{\link[=coherent_dist]{coherent_dist()}}: general coherent system given minimal path sets +
components
\item Topology shortcuts: \code{\link[=series_dist]{series_dist()}}, \code{\link[=parallel_dist]{parallel_dist()}}, \code{\link[=kofn_dist]{kofn_dist()}},
\code{\link[=bridge_dist]{bridge_dist()}}, \code{\link[=consecutive_k_dist]{consecutive_k_dist()}}
\item IID convenience: \code{\link[=min_iid]{min_iid()}}, \code{\link[=max_iid]{max_iid()}}, \code{\link[=order_statistic]{order_statistic()}}
}

Closed-form specializations (exp_series, wei_series,
wei_homogeneous_series) and richer importance measures
(birnbaum_importance, criticality_importance) are deferred to later
releases.
}

\seealso{
Useful links:
\itemize{
  \item \url{https://github.com/queelius/dist.structure}
  \item Report bugs at \url{https://github.com/queelius/dist.structure/issues}
}

}
\author{
\strong{Maintainer}: Alexander Towell \email{lex@metafunctor.com} (\href{https://orcid.org/0000-0001-6443-9897}{ORCID})

}
\keyword{internal}
