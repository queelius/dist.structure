% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cold_standby.R
\name{mean.cold_standby_dist}
\alias{mean.cold_standby_dist}
\title{Mean of a cold-standby system: sum of component means}
\usage{
\method{mean}{cold_standby_dist}(x, ...)
}
\arguments{
\item{x}{A \code{cold_standby_dist} object.}

\item{...}{Passed to the Monte Carlo fallback as \code{mc} (default 1e5).}
}
\value{
Numeric scalar.
}
\description{
Computes \verb{sum_j E[T_j]} exactly when every component implements a
\code{mean()} method. Falls back to a Monte Carlo estimate from the
sampler when any component lacks an exact mean (e.g., a
\code{dist_structure} component whose \code{mean} would route through
\code{algebraic.dist::univariate_dist} and require \code{density} / \code{sup},
which are not provided on \code{dist_structure} by default).
}
